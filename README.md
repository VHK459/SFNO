## SFNO Training
This repo contains all the required files to train a sfno model from scratch.

`config.py` contains all the necessary hyperparameters along with training variables.

`phase1.py` is the usual 6h prediction task.

`phase3.py` is for curricullum training, and `phase4.py` for regional finetuning.
